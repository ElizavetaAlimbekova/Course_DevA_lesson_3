import OS.SystemOperation;
import cpu.CPU;
import memory.SSD;

public class Macbook extends Computer {

    public Macbook (CPU cpu, SSD ssd, SystemOperation os) {
        super (cpu, ssd,os);
    }


}
